<!-- GENERATED FILE: scripts/tasks/operation-catalog --write -->
# Operation support matrix

Catalog version: `1.0.0`; schema version: `1`.

| Operation ID | Linux | macOS | Windows |
|---|---|---|---|
| `op.battery-add` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.battery-inspect` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.battery-install` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | unsupported — Battery repository installation and cache operations are Unix-only. |
| `op.battery-list` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.battery-remove` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.battery-scripts` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.battery-sync` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-api` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-completion` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-help-ai` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-history-tail` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-init` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-node-authority-create` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-node-authority-issue` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-node-authority-show` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-node-baseline-create-key` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-node-baseline-publish` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-node-direct-probe` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-node-reset` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-node-serve` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-queue-worker` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-run` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-serve` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-token-generate` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-trace` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-uninstall` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.cli-update` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.config` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.describe` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.doctor` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-activate` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-create` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-deactivate` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-delete` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-list` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-remove` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-replace` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-set` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.env-show` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.history-list` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.history-show` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.history-stats` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.history-traces` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.http-admin-status` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.http-health` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.http-node-enrollments` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.http-ready` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.http-secrets` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.http-tree-path` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.http-tree-root` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.http-workspace` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-baseline-push` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-baseline-rollback` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-capabilities` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-cue` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-discovery` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | unsupported — Discovery is unsupported on Windows; the bounded scanner has no Windows implementation. |
| `op.node-enroll-apply` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-enroll-approve` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-enroll-reject` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-enroll-request` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-health` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-init` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-peers` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-revoke` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-signals` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-status` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.node-trust` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.queue-add` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.queue-cancel` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.queue-dead-letter` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.scripts` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |
| `op.search` | supported — Supported by the headless Rust runtime and adapter contract. | supported — Supported by the headless Rust runtime; platform-specific integration remains static. | supported — Supported by the headless Rust runtime and adapter contract. |

Total operations: 72.
