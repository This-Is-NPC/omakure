.TH OMAKURE 1
.SH NAME
omakure \- Omakure \- CLI for running and scheduling automation scripts
.SH SYNOPSIS
\fBomakure\fR [OPTIONS] [COMMAND]
.SH DESCRIPTION
Omakure \- CLI for running and scheduling automation scripts.
.PP
Run `omakure` with no arguments to print this help.
.PP
CLI surfaces:
 run <SCRIPT>          execute a script directly
 queue add <SCRIPT>    push a job; `queue worker` drains it
 serve                 run the cron scheduler daemon
 history list|show     query past runs (SQLite\-backed)
 scripts|describe|search   inspect the script catalogue
.PP
AI integration: pass `\-\-json` on supported subcommands to emit a `{ ok, data, error, schema_version }` envelope; run `omakure help\-ai` for the full machine\-readable capability surface.
.PP
.SH OPTIONS
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH COMMANDS
.TP
\fBrun\fR
Run a script directly
.TP
\fBdoctor\fR
Check runtime dependencies and workspace
.RS
\fIAliases: \fRcheck
.RE
.TP
\fBscripts\fR
List available scripts
.TP
\fBdescribe\fR
Show the full schema of one script
.TP
\fBsearch\fR
Search the script index
.TP
\fBhistory\fR
Query the run history
.TP
\fBhistory list\fR
List recent runs
.TP
\fBhistory show\fR
Show one run by id
.TP
\fBhistory tail\fR
Print the most recent N runs (no \-\-follow in v1)
.TP
\fBhistory stats\fR
Aggregate counts per state and per actor
.TP
\fBhistory traces\fR
Read the structured trace stream of one run
.TP
\fBqueue\fR
Push, cancel, drain, and inspect the run queue
.TP
\fBqueue add\fR
Push a job onto the queue
.TP
\fBqueue cancel\fR
Cancel a queued or running job
.TP
\fBqueue dead\-letter\fR
Promote a `failed` or `timed_out` row into `dead_letter`
.TP
\fBqueue worker\fR
Drain the queue (long\-running daemon)
.TP
\fBqueue stats\fR
Aggregate counts per state and per actor
.TP
\fBbattery\fR
Manage reusable Battery automation repositories
.TP
\fBbattery list\fR
List registered Batteries
.TP
\fBbattery add\fR
Register a Battery repository source
.TP
\fBbattery sync\fR
Fetch and validate a Battery checkout
.TP
\fBbattery inspect\fR
Inspect one synced Battery manifest
.TP
\fBbattery scripts\fR
List installable scripts from one Battery
.TP
\fBbattery install\fR
Install one Battery script into the trusted scripts workspace
.TP
\fBbattery remove\fR
Unregister one Battery
.TP
\fBtoken\fR
Generate hashed API tokens for `\-\-tokens\-file` auth
.TP
\fBtoken generate\fR
Generate a plaintext token, Argon2id hash, and TOML entry
.TP
\fBapi\fR
Run the internal HTTP management API
.TP
\fBtrace\fR
Run the machine\-owned node service (HTTP API + optional workers + scheduler)
.TP
\fBhelp\-ai\fR
Print the AI capability surface as JSON
.TP
\fBinit\fR
Create a new script template
.TP
\fBenv\fR
Manage named environment files
.TP
\fBenv list\fR
List named environments
.TP
\fBenv create\fR
Create a named environment from optional `KEY=value` pairs
.TP
\fBenv show\fR
Show a named environment with sensitive values redacted
.TP
\fBenv set\fR
Set one `KEY=value` in a named environment
.TP
\fBenv remove\fR
Remove one key from a named environment
.TP
\fBenv replace\fR
Replace a named environment with the provided `KEY=value` pairs
.TP
\fBenv activate\fR
Activate a named environment
.TP
\fBenv deactivate\fR
Deactivate the current environment
.TP
\fBenv delete\fR
Delete a named environment
.TP
\fBnode\fR
Inspect and explicitly manage the machine\-owned node identity and trust registry
.TP
\fBnode serve\fR
Run the machine\-owned HTTP node service with optional workers and scheduler
.TP
\fBnode direct\-probe\fR
Establish a direct encrypted probe with one explicitly trusted peer
.TP
\fBnode cue\fR
Ask one trusted Performer to run a script it has already declared
.TP
\fBnode baseline\fR
Publish and deliver the signed set of scripts a fleet runs
.TP
\fBnode baseline create\-key\fR
Create this node's baseline publisher key, refusing to replace one
.TP
\fBnode baseline publish\fR
Sign the named workspace scripts as one baseline
.TP
\fBnode baseline push\fR
Deliver a signed baseline to one trusted Performer
.TP
\fBnode baseline rollback\fR
Put this node back on the one baseline retained before the current one
.TP
\fBnode init\fR
Explicitly initialize public config, identity, and local trust state
.TP
\fBnode status\fR
Inspect public node identity, redacted config, and bounded trust counts
.TP
\fBnode peers\fR
List registered peers without audit history or private state
.TP
\fBnode health\fR
Show current fleet health: presence, profile, and runner status
.TP
\fBnode signals\fR
Show the bounded newest\-first closed Signal feed: enrolled, revoked, run\-completed
.TP
\fBnode discovery\fR
Run one bounded in\-memory LAN discovery scan
.TP
\fBnode trust\fR
Explicitly import and activate one manually trusted peer
.TP
\fBnode enroll\fR
Request and explicitly approve or reject manual enrollment
.TP
\fBnode enroll request\fR
Create and send one signed manual enrollment request
.TP
\fBnode enroll approve\fR
Approve one pending request after checking the out\-of\-band code
.TP
\fBnode enroll reject\fR
Reject one pending request without activating trust
.TP
\fBnode enroll apply\fR
Apply one authority\-signed unattended enrollment bundle
.TP
\fBnode authority\fR
Hold and use this node's enrollment authority
.TP
\fBnode authority create\fR
Create this node's enrollment authority key, refusing to replace one
.TP
\fBnode authority show\fR
Report the authority this node holds, without its private half
.TP
\fBnode authority issue\fR
Mint one enrollment bundle naming this node as the subject
.TP
\fBnode capabilities\fR
Update one peer's capability allow\-list with confirmation and evidence
.TP
\fBnode revoke\fR
Revoke one peer with confirmation and evidence
.TP
\fBnode reset\fR
Explicitly remove validated machine identity and node trust state
.TP
\fBconfig\fR
Show resolved paths and environment diagnostics
.TP
\fBupdate\fR
Update omakure from GitHub releases
.TP
\fBuninstall\fR
Remove the omakure binary (optionally wipe the scripts workspace)
.TP
\fBcompletion\fR
Generate shell completion script for the given shell
.TP
\fBserve\fR
Run the cron scheduler daemon for scripts declaring a `Schedule` block
.SH "OMAKURE RUN"
Run a script directly

Without `\-\-no\-prompt` or `\-\-json`, missing required schema fields prompt on stdin or a TTY. Scripts generated with `omakure init` may also prompt for values such as an optional `target`. When stdin is not a TTY (closed stdin, pipes), those prompts can fail with exit code 1 and no extra omakure diagnostic — pass arguments after `\-\-` or use `\-\-no\-prompt`.
.PP
\fBUsage:\fR omakure run [OPTIONS] <SCRIPT> [<ARGS>] ...
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-actor\fR \fI<ACTOR>\fR
Actor tag recorded in the run history (default: `human`)
.RS
\fIDefault: \fRhuman
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Optional free\-form reason recorded in the run history
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-run\-id\fR \fI<RUN_ID>\fR
Caller\-provided run id; otherwise a fresh id is generated
.TP
\fB\-\-parent\-run\-id\fR \fI<PARENT_RUN_ID>\fR
Optional parent run id, for chained agent workflows
.TP
\fB\-\-no\-prompt\fR
Fail with a structured error when required schema fields are missing instead of prompting on stdin or a TTY. Implied by `\-\-json`. Does not disable prompts embedded in the script itself (for example `omakure init` templates that read optional values); for non\-interactive runs pass arguments after `\-\-` or use this flag and supply every required value
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-env\-file\fR \fI<PATH>\fR
Path to an env file whose `KEY=value` pairs are injected into the script process for this run only. Values override the managed active env for the same key, but omakure\-reserved vars (`OMAKURE_RUN_ID`, `OMAKURE_SCRIPTS_DIR`) always win. A missing or unreadable path is a hard error.

Example: `omakure run deploy \-\-env\-file ./.venv.env \-\- \-\-target prod`
.TP
\fB\-\-secret\fR \fI<FIELD=VALUE>\fR
Direct secret field input as `FIELD=value`. The value is supplied to secret schema fields for this run and is redacted from stored args
\fBArguments:\fR
.PP
.TP
\fB<SCRIPT>\fR
Script name or path
.TP
\fB<ARGS>\fR
Arguments forwarded to the script
.SH "OMAKURE DOCTOR"
Check runtime dependencies and workspace

Verifies required interpreters (`git`, `bash`, `jq`), optional ones (`powershell`, `python`), workspace layout (`.omakure/`, history dir, workspace config), and that every script's embedded schema parses. Exits 1 if any required check fails. `\-\-json` is currently ignored by this subcommand.
.PP
\fBUsage:\fR omakure doctor [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE SCRIPTS"
List available scripts
.PP
\fBUsage:\fR omakure scripts [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-tag\fR \fI<TAG>\fR
Filter by tag (repeatable; AND semantics, case\-sensitive literal match against the script's embedded `Tags` field)
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE DESCRIBE"
Show the full schema of one script
.PP
\fBUsage:\fR omakure describe [OPTIONS] <SCRIPT>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<SCRIPT>\fR
Script name or path
.SH "OMAKURE SEARCH"
Search the script index
.PP
\fBUsage:\fR omakure search [OPTIONS] [<QUERY>]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-tag\fR \fI<TAG>\fR
Filter by tag (repeatable; AND semantics, case\-sensitive literal match against the script's embedded `Tags` field)
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<QUERY>\fR
Free\-text query (matches name, description, tags, fields)
.RS
\fIDefault: \fR
.RE
.SH "OMAKURE HISTORY"
Query the run history
.PP
\fBUsage:\fR omakure history [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE HISTORY LIST"
List recent runs
.PP
\fBUsage:\fR omakure history list [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-script\fR \fI<SCRIPT>\fR
Filter by script name or path substring
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-actor\fR \fI<ACTOR>\fR
Filter by actor tag (e.g. `human`, `ai`)
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-since\fR \fI<SINCE>\fR
Only runs since this duration ago (e.g. `1d`, `30m`, `12h`)
.TP
\fB\-\-until\fR \fI<UNTIL>\fR
Only runs until this duration ago
.TP
\fB\-\-success\fR
Only successful runs
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-failure\fR
Only failed runs
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-limit\fR \fI<LIMIT>\fR
Maximum number of rows to return
.TP
\fB\-\-state\fR \fI<STATE>\fR
Filter by run state (repeatable; logical OR within the flag). Valid values: queued, running, completed, failed, cancelled, timed_out, dead_letter. Mutually exclusive with `\-\-state\-set`
.TP
\fB\-\-state\-set\fR \fI<STATE_SET>\fR
Filter by a named state group: `in_flight` (queued+running), `terminal` (everything else), or `all`. Default when neither `\-\-state` nor `\-\-state\-set` is set: `terminal` so existing callers see no behavior change
.SH "OMAKURE HISTORY SHOW"
Show one run by id
.PP
\fBUsage:\fR omakure history show [OPTIONS] <RUN_ID>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<RUN_ID>\fR
Run id (as printed by `omakure run \-\-json` or `omakure history list`)
.SH "OMAKURE HISTORY TAIL"
Print the most recent N runs (no \-\-follow in v1)
.PP
\fBUsage:\fR omakure history tail [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-limit\fR \fI<LIMIT>\fR
Number of rows to print (default: 10)
.RS
\fIDefault: \fR10
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-follow\fR
Unsupported; rejected with error.code = "not_implemented"
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE HISTORY STATS"
Aggregate counts per state and per actor
.PP
\fBUsage:\fR omakure history stats [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE HISTORY TRACES"
Read the structured trace stream of one run
.PP
\fBUsage:\fR omakure history traces [OPTIONS] <RUN_ID>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-level\fR \fI<LEVEL>\fR
Minimum level (debug, info, warn, error). Defaults to `debug` (returns every record)
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-since\-sequence\fR \fI<SINCE_SEQUENCE>\fR
Return only entries with `sequence > N`. Used by agents for incremental fetches
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<RUN_ID>\fR
Run id
.SH "OMAKURE QUEUE"
Push, cancel, drain, and inspect the run queue
.PP
\fBUsage:\fR omakure queue [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE QUEUE ADD"
Push a job onto the queue
.PP
\fBUsage:\fR omakure queue add [OPTIONS] <SCRIPT> [<ARGS>] ...
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-actor\fR \fI<ACTOR>\fR
Actor tag recorded on the row (default: `human`)
.RS
\fIDefault: \fRhuman
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Optional free\-form reason
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-priority\fR \fI<PRIORITY>\fR
Higher value picked first (default 0)
.RS
\fIDefault: \fR0
.RE
.TP
\fB\-\-timeout\fR \fI<TIMEOUT>\fR
Per\-job execution timeout (e.g. `30s`, `5m`, `1h`). Without this flag the job has no execution limit
.TP
\fB\-\-parent\-run\-id\fR \fI<PARENT_RUN_ID>\fR
Optional parent run id, for chained agent workflows
.TP
\fB\-\-run\-id\fR \fI<RUN_ID>\fR
Caller\-provided run id; otherwise a fresh id is generated
.TP
\fB\-\-cron\-schedule\-id\fR \fI<CRON_SCHEDULE_ID>\fR
Provenance id tying this row to a named cron schedule. Populated automatically by `omakure serve`; set manually only to replay or simulate a scheduled run
\fBArguments:\fR
.PP
.TP
\fB<SCRIPT>\fR
Script name or path
.TP
\fB<ARGS>\fR
Arguments forwarded to the script (after `\-\-`)
.SH "OMAKURE QUEUE CANCEL"
Cancel a queued or running job
.PP
\fBUsage:\fR omakure queue cancel [OPTIONS] <RUN_ID>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Optional reason recorded on the cancelled row
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<RUN_ID>\fR
Run id to cancel
.SH "OMAKURE QUEUE DEAD-LETTER"
Promote a `failed` or `timed_out` row into `dead_letter`
.PP
\fBUsage:\fR omakure queue dead\-letter [OPTIONS] <RUN_ID>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Optional reason appended to the row
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<RUN_ID>\fR
Run id to promote
.SH "OMAKURE QUEUE WORKER"
Drain the queue (long\-running daemon)
.PP
\fBUsage:\fR omakure queue worker [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-concurrency\fR \fI<CONCURRENCY>\fR
Number of parallel workers (default 1)
.RS
\fIDefault: \fR1
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-actor\-filter\fR \fI<ACTOR_FILTER>\fR
Only claim jobs whose actor matches this tag
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-script\-filter\fR \fI<SCRIPT_FILTER>\fR
Only claim jobs whose script path or name contains this pattern
.TP
\fB\-\-once\fR
Test convenience: drain at most one job per worker thread, then exit. Hidden from \-\-help and help\-ai. Used by integration tests so the daemon does not block the test harness
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE QUEUE STATS"
Aggregate counts per state and per actor
.PP
\fBUsage:\fR omakure queue stats [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE BATTERY"
Manage reusable Battery automation repositories
.PP
\fBUsage:\fR omakure battery [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE BATTERY LIST"
List registered Batteries
.PP
\fBUsage:\fR omakure battery list [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE BATTERY ADD"
Register a Battery repository source
.PP
\fBUsage:\fR omakure battery add [OPTIONS] <GIT_URL>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-name\fR \fI<NAME>\fR
Stable Battery name (lowercase kebab\-case)
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-ref\fR \fI<REQUESTED_REF>\fR
Branch, tag, or ref to sync
.RS
\fIDefault: \fRmain
.RE
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-token\-ref\fR \fI<TOKEN_REF>\fR
Secret ref for private HTTPS auth (`secret://provider/key`). Registry stores the ref only; sync resolves via GIT_ASKPASS
\fBArguments:\fR
.PP
.TP
\fB<GIT_URL>\fR
Git repository URL or local path
.SH "OMAKURE BATTERY SYNC"
Fetch and validate a Battery checkout
.PP
\fBUsage:\fR omakure battery sync [OPTIONS] <NAME>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<NAME>\fR
Battery name
.SH "OMAKURE BATTERY INSPECT"
Inspect one synced Battery manifest
.PP
\fBUsage:\fR omakure battery inspect [OPTIONS] <NAME>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<NAME>\fR
Battery name
.SH "OMAKURE BATTERY SCRIPTS"
List installable scripts from one Battery
.PP
\fBUsage:\fR omakure battery scripts [OPTIONS] <NAME>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<NAME>\fR
Battery name
.SH "OMAKURE BATTERY INSTALL"
Install one Battery script into the trusted scripts workspace
.PP
\fBUsage:\fR omakure battery install [OPTIONS] <NAME> <SCRIPT_ID>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-force\fR
Overwrite an existing script target
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<NAME>\fR
Battery name
.TP
\fB<SCRIPT_ID>\fR
Script id from `omakure battery scripts <name>`
.SH "OMAKURE BATTERY REMOVE"
Unregister one Battery
.PP
\fBUsage:\fR omakure battery remove [OPTIONS] <NAME>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-remove\-cache\fR
Also delete the cached clone
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<NAME>\fR
Battery name
.SH "OMAKURE TOKEN"
Generate hashed API tokens for `\-\-tokens\-file` auth

Prints a plaintext token once (prefix `omk_live_`), its Argon2id PHC hash, and a TOML `[[tokens]]` entry. Does not append to a secrets file unless `\-\-append` is passed with `\-\-confirmed`.
.PP
\fBUsage:\fR omakure token [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE TOKEN GENERATE"
Generate a plaintext token, Argon2id hash, and TOML entry
.PP
\fBUsage:\fR omakure token generate [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-id\fR \fI<ID>\fR
Stable token id (logged/audited; never the secret)
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-scope\fR \fI<SCOPES>\fR
Scope to grant (repeatable), e.g. runs:read, scripts:read, *
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-append\fR \fI<APPEND>\fR
Append the TOML entry to this tokens file (requires `\-\-confirmed`)
.TP
\fB\-\-confirmed\fR
Confirm a destructive/automated `\-\-append`
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE API"
Run the internal HTTP management API

Starts a loopback\-only HTTP API by default at `127.0.0.1:7878`. All endpoints except `/v1/health` and `/v1/ready` require `Authorization: Bearer <token>`. Prefer `\-\-tokens\-file` / `OMAKURE_TOKENS_FILE` (per\-token Argon2id scopes). Legacy `OMAKURE_API_TOKEN` still works when no tokens file is configured. Binding to non\-loopback addresses requires `\-\-allow\-non\-loopback`.
.PP
\fBUsage:\fR omakure api [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-bind\fR \fI<BIND>\fR
Address to bind the HTTP API server to
.RS
\fIDefault: \fR127.0.0.1:7878
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-allow\-non\-loopback\fR
Explicitly allow the HTTP API to bind to non\-loopback addresses
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-policy\fR \fI<POLICY>\fR
Deploy\-only policy.toml (route groups + auth/node\-service defaults). Overrides `OMAKURE_POLICY_FILE`. Separate from workspace omakure.toml
.TP
\fB\-\-tokens\-file\fR \fI<TOKENS_FILE>\fR
Multi\-token TOML file (Argon2id hashes + per\-token scopes). Overrides `OMAKURE_TOKENS_FILE`. When set, process\-wide `\-\-capability` is ignored; scopes come from each token
.TP
\fB\-\-capability\fR \fI<CAPABILITIES>\fR
API capability to grant in legacy single\-token mode (`OMAKURE_API_TOKEN`). Repeatable. Ignored when `\-\-tokens\-file` is set. Supported: config:read, scripts:read, env:read / envs:read, env:write / envs:write, env:activate / envs:activate, env:use / envs:use, secrets:use, secrets:read\-metadata, credentials:use, runs:read, runs:write / runs:enqueue, batteries:read, batteries:write, admin:status, all. Node management uses narrow node:read, node:write, and trust:write capabilities. `all` grants every route capability but does not bypass `\-\-secret\-ref` (pass `\-\-secret\-ref '*'` for unrestricted refs)
.TP
\fB\-\-secret\-ref\fR \fI<SECRET_REFS>\fR
Allowed secret provider ref for secrets:use / credentials:use, e.g. secret://prod/token or secret://prod/*; repeatable. Empty denies provider refs
.SH "OMAKURE TRACE"
Run the machine\-owned node service (HTTP API + optional workers + scheduler)

Starts the HTTP management API and optionally embeds queue workers and the existing schedule scanner in one process. Use `\-\-workers 0 \-\-no\-scheduler` for API\-only (same auth surface as `omakure api`). `GET /v1/ready` is unauthenticated and returns minimal readiness. `GET /v1/admin/status` (scope `admin:status`) exposes readiness details and token reload health without secrets. Authenticated requests emit `omakure.http_audit` lines with `token_id` (Authorization redacted). SIGTERM/SIGINT stops HTTP first, then scheduling/claiming, then drains workers. Append a structured trace event from inside a running script
.PP
\fBUsage:\fR omakure trace [OPTIONS] <MESSAGE>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-level\fR \fI<LEVEL>\fR
Level (debug, info, warn, error). Defaults to `info`
.RS
\fIDefault: \fRinfo
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-data\fR \fI<DATA>\fR
Optional structured payload (must parse as JSON)
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<MESSAGE>\fR
Trace message
.SH "OMAKURE HELP-AI"
Print the AI capability surface as JSON

Always emits JSON (regardless of `\-\-json`). The envelope uses the standard `{ ok, data, error, schema_version }` shape.

 `data` contains:
 trust_model   — how omakure treats AI callers
 error_codes   — the registered stable error code strings
 envelope      — a self\-describing shape hint
 verbs         — AI\-relevant subcommands with flags and nested subcommands (pulled from clap metadata, so it cannot drift from `\-\-help`)
 data_shapes   — concrete examples for `run`, `history_list`, `history_show`, and `config`

Agents can cache the payload per binary version (`\-\-version`).
.PP
\fBUsage:\fR omakure help\-ai [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE INIT"
Create a new script template
.PP
\fBUsage:\fR omakure init [OPTIONS] [<SCRIPT>]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-name\fR \fI<SCRIPT>\fR
Script path (legacy)
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-schema\-json\fR \fI<SCHEMA_JSON>\fR
Inline schema JSON or `@path/to/schema.json`. When set, the new script is generated with this schema embedded between the `OMAKURE_SCHEMA_START` / `OMAKURE_SCHEMA_END` markers instead of the default placeholder template
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-body\-stdin\fR
Read the script body from stdin and write it verbatim under the schema header when `\-\-schema\-json` is set. Without `\-\-schema\-json`, stdin is ignored and the default placeholder template is written
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-force\fR
Overwrite an existing script of the same name
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<SCRIPT>\fR
Script path
.SH "OMAKURE ENV"
Manage named environment files
.PP
\fBUsage:\fR omakure env [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV LIST"
List named environments
.PP
\fBUsage:\fR omakure env list [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV CREATE"
Create a named environment from optional `KEY=value` pairs
.PP
\fBUsage:\fR omakure env create [OPTIONS] <NAME> [<KEY=VALUE>] ...
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV SHOW"
Show a named environment with sensitive values redacted
.PP
\fBUsage:\fR omakure env show [OPTIONS] <NAME>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV SET"
Set one `KEY=value` in a named environment
.PP
\fBUsage:\fR omakure env set [OPTIONS] <NAME> <KEY=VALUE>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV REMOVE"
Remove one key from a named environment
.PP
\fBUsage:\fR omakure env remove [OPTIONS] <NAME> <KEY>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV REPLACE"
Replace a named environment with the provided `KEY=value` pairs
.PP
\fBUsage:\fR omakure env replace [OPTIONS] <NAME> [<KEY=VALUE>] ...
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV ACTIVATE"
Activate a named environment
.PP
\fBUsage:\fR omakure env activate [OPTIONS] <NAME>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV DEACTIVATE"
Deactivate the current environment
.PP
\fBUsage:\fR omakure env deactivate [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE ENV DELETE"
Delete a named environment
.PP
\fBUsage:\fR omakure env delete [OPTIONS] <NAME>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE"
Inspect and explicitly manage the machine\-owned node identity and trust registry
.PP
\fBUsage:\fR omakure node [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-node\-state\-dir\fR \fI<STATE_DIR>\fR
Deterministic test\-only node state directory override
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-node\-config\fR \fI<CONFIG_PATH>\fR
Deterministic test\-only node configuration path override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE SERVE"
Run the machine\-owned HTTP node service with optional workers and scheduler

On Linux and macOS, production node configuration and state use machine\-owned paths (`/etc/omakure` and `/var/lib/omakure` on Linux). A per\-user binary install does not provision them; use the machine\-service installer (`sudo … \-\-install\-node\-service`) so those directories exist and `node serve` runs under the service account.
.PP
\fBUsage:\fR omakure node serve [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-bind\fR \fI<BIND>\fR
Address to bind the HTTP API server to; defaults to node.toml `api.bind`
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-direct\-bind\fR \fI<DIRECT_BIND>\fR
Optional direct transport listener address
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-allow\-non\-loopback\fR
Explicitly allow binding to non\-loopback addresses
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-allow\-non\-loopback\-direct\fR
Explicitly allow the direct transport to bind to non\-loopback addresses
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-policy\fR \fI<POLICY>\fR
Deploy\-only policy.toml. Same as `omakure api \-\-policy`
.TP
\fB\-\-workers\fR \fI<WORKERS>\fR
Number of embedded queue workers. `0` means API\-only (no claiming)
.TP
\fB\-\-scheduler\fR
Explicitly enable the in\-process schedule scanner
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-no\-scheduler\fR
Disable the in\-process schedule scanner
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-worker\-actor\-filter\fR \fI<WORKER_ACTOR_FILTER>\fR
Only claim jobs whose actor matches this tag
.TP
\fB\-\-worker\-script\-filter\fR \fI<WORKER_SCRIPT_FILTER>\fR
Only claim jobs whose script path or name contains this pattern
.TP
\fB\-\-readiness\-requires\-worker\fR
Fail `/v1/ready` when configured workers are not alive
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-readiness\-requires\-scheduler\fR
Fail `/v1/ready` when the scheduler is enabled but not alive
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-readiness\-requires\-transport\fR
Fail `/v1/ready` while configured static peers are not connected
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-tokens\-file\fR \fI<TOKENS_FILE>\fR
Multi\-token TOML file. Same as `omakure api \-\-tokens\-file`
.TP
\fB\-\-capability\fR \fI<CAPABILITIES>\fR
API capability to grant in legacy single\-token mode. Repeatable
.TP
\fB\-\-secret\-ref\fR \fI<SECRET_REFS>\fR
Allowed secret provider ref for secrets:use. Same as `omakure api \-\-secret\-ref`
.TP
\fB\-\-bootstrap\-token\-file\fR \fI<BOOTSTRAP_TOKEN_FILE>\fR
Node\-local one\-time bootstrap token file for the signed\-bundle API
.SH "OMAKURE NODE DIRECT-PROBE"
Establish a direct encrypted probe with one explicitly trusted peer
.PP
\fBUsage:\fR omakure node direct\-probe [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-endpoint\fR \fI<ENDPOINT>\fR
Peer direct transport address
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-peer\-node\-id\fR \fI<PEER_NODE_ID>\fR
Expected canonical peer node ID
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE CUE"
Ask one trusted Performer to run a script it has already declared
.PP
\fBUsage:\fR omakure node cue [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-endpoint\fR \fI<ENDPOINT>\fR
Peer direct transport address
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-peer\-node\-id\fR \fI<PEER_NODE_ID>\fR
Expected canonical peer node ID
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-script\fR \fI<SCRIPT>\fR
Script name as the Performer declared it. A path is not accepted: the Performer resolves the name against what it published, and a Cue never carries a location
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Why this is being asked for. Recorded in the Performer's audit trail
.TP
\fB\-\-wait\-seconds\fR \fI<WAIT_SECONDS>\fR
How long to stay on the session waiting for the `run\-completed` Signal.

The outcome is read on the connection this dial already opened, because a Performer that holds a standing session with this Conductor refuses the dial outright — the configuration that would deliver the Signal is the one in which the Cue could not be sent. `0` dispatches and returns immediately; the run still happens and still reports.
.RS
\fIDefault: \fR120
.RE
.TP
\fB\-\-direct\fR
Dial the peer from this process instead of asking the running service.

The service is preferred because it is the only thing that can reach a peer this node already has a session with. Use this for a peer there is no standing session with, or when no service is running.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-cue\-id\fR \fI<CUE_ID>\fR
Caller\-supplied Cue id (32 lowercase hex). Omit to mint a new one.

The same id retries the same instruction; a new id is a new run.
.SH "OMAKURE NODE BASELINE"
Publish and deliver the signed set of scripts a fleet runs
.PP
\fBUsage:\fR omakure node baseline [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE BASELINE CREATE-KEY"
Create this node's baseline publisher key, refusing to replace one
.PP
\fBUsage:\fR omakure node baseline create\-key [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE BASELINE PUBLISH"
Sign the named workspace scripts as one baseline
.PP
\fBUsage:\fR omakure node baseline publish [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-script\fR \fI<PATH>\fR
A workspace\-relative script path to include. Repeatable.

A path rather than a bare name, because a baseline says *where* each script goes on every receiver; a name would leave that to whatever the receiver happened to guess.
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-lifetime\-seconds\fR \fI<LIFETIME_SECONDS>\fR
How long the baseline stays installable, in seconds
.RS
\fIDefault: \fR3600
.RE
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-out\fR \fI<PATH>\fR
Where to write the signed manifest
.SH "OMAKURE NODE BASELINE PUSH"
Deliver a signed baseline to one trusted Performer
.PP
\fBUsage:\fR omakure node baseline push [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-peer\-node\-id\fR \fI<PEER_NODE_ID>\fR
Expected canonical peer node ID
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-manifest\fR \fI<PATH>\fR
The signed manifest produced by `node baseline publish`
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-wait\-seconds\fR \fI<WAIT_SECONDS>\fR
How long to wait on the session for the Performer's answer
.RS
\fIDefault: \fR120
.RE
.SH "OMAKURE NODE BASELINE ROLLBACK"
Put this node back on the one baseline retained before the current one

A local operator action, not something a Conductor orders. The baseline plane carries exactly two message kinds and neither of them is "run the other version"; a remote rollback verb would hand a Conductor the power to flip a Performer between two code versions at will, which is a power the split between publishing and conducting exists to withhold. The drift status on `node health` says which machine to go and fix.

Exactly one previous baseline is retained, and this is a swap: rolling back twice returns this node to where it started. The retained set is re\-verified against the publishers this node names *today*, so a publisher revoked since the original install makes the rollback fail.
.PP
\fBUsage:\fR omakure node baseline rollback [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-confirmed\fR
Required. A rollback replaces every script the current baseline named
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE INIT"
Explicitly initialize public config, identity, and local trust state

On Linux and macOS, production paths default to machine\-owned directories (`/etc/omakure/node.toml` and `/var/lib/omakure` on Linux). A per\-user install does not create them; initializing as an unprivileged user against those paths fails with permission denied.
.PP
\fBUsage:\fR omakure node init [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE STATUS"
Inspect public node identity, redacted config, and bounded trust counts
.PP
\fBUsage:\fR omakure node status [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE PEERS"
List registered peers without audit history or private state
.PP
\fBUsage:\fR omakure node peers [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE HEALTH"
Show current fleet health: presence, profile, and runner status
.PP
\fBUsage:\fR omakure node health [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE SIGNALS"
Show the bounded newest\-first closed Signal feed: enrolled, revoked, run\-completed
.PP
\fBUsage:\fR omakure node signals [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE DISCOVERY"
Run one bounded in\-memory LAN discovery scan
.PP
\fBUsage:\fR omakure node discovery [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-wait\-seconds\fR \fI<WAIT_SECONDS>\fR
Discovery scan duration in seconds, bounded to 1..=30
.RS
\fIDefault: \fR5
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-include\-addresses\fR
Include observed source addresses in the local CLI result
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE TRUST"
Explicitly import and activate one manually trusted peer
.PP
\fBUsage:\fR omakure node trust [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-node\-id\fR \fI<NODE_ID>\fR
Canonical omk1_ node identifier
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-public\-key\fR \fI<PUBLIC_KEY>\fR
Lowercase hexadecimal x\-only BIP\-340 public key
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-transport\-certificate\fR \fI<TRANSPORT_CERTIFICATE>\fR
Signed transport certificate as lowercase hexadecimal bytes
.TP
\fB\-\-role\fR \fI<ROLE>\fR
Peer role: conductor or performer
.RS
\fIDefault: \fRperformer
.RE
.TP
\fB\-\-capability\fR \fI<CAPABILITIES>\fR
Allowed capability (repeatable; sorted unique values are required)
.TP
\fB\-\-actor\fR \fI<ACTOR>\fR
Audit actor
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Audit reason/evidence
.TP
\fB\-\-confirmed\fR
Confirm this trust mutation explicitly
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE ENROLL"
Request and explicitly approve or reject manual enrollment
.PP
\fBUsage:\fR omakure node enroll [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE ENROLL REQUEST"
Create and send one signed manual enrollment request
.PP
\fBUsage:\fR omakure node enroll request [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-endpoint\fR \fI<ENDPOINT>\fR
Peer direct transport address
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-role\fR \fI<ROLE>\fR
Requested peer role
.RS
\fIDefault: \fRperformer
.RE
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-capability\fR \fI<CAPABILITIES>\fR
Requested capability (repeatable; sorted unique values are required)
.TP
\fB\-\-lifetime\-seconds\fR \fI<LIFETIME_SECONDS>\fR
Request lifetime in seconds, at most 30 days
.RS
\fIDefault: \fR600
.RE
.SH "OMAKURE NODE ENROLL APPROVE"
Approve one pending request after checking the out\-of\-band code
.PP
\fBUsage:\fR omakure node enroll approve [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-request\fR \fI<REQUEST_HEX>\fR
Exact signed OMMA request as lowercase hexadecimal bytes
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-transport\-certificate\fR \fI<TRANSPORT_CERTIFICATE>\fR
Candidate transport certificate as lowercase hexadecimal bytes
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-code\fR \fI<CODE>\fR
Out\-of\-band 16\-byte approval code as lowercase hexadecimal
.TP
\fB\-\-actor\fR \fI<ACTOR>\fR
Audit actor
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Audit reason/evidence
.TP
\fB\-\-confirmed\fR
Confirm this trust mutation explicitly
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE ENROLL REJECT"
Reject one pending request without activating trust
.PP
\fBUsage:\fR omakure node enroll reject [OPTIONS] <NODE_ID>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-actor\fR \fI<ACTOR>\fR
Audit actor
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Audit reason/evidence
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-confirmed\fR
Confirm this denial explicitly
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<NODE_ID>\fR
Pending candidate node identifier
.SH "OMAKURE NODE ENROLL APPLY"
Apply one authority\-signed unattended enrollment bundle
.PP
\fBUsage:\fR omakure node enroll apply [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-bundle\-file\fR \fI<BUNDLE_FILE>\fR
Exact signed OMEB bundle file. The file is never echoed or persisted
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-bootstrap\-token\-file\fR \fI<BOOTSTRAP_TOKEN_FILE>\fR
One\-time bootstrap token file. The token is never echoed or persisted
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-bootstrap\-nonce\fR \fI<BOOTSTRAP_NONCE>\fR
One\-time 16\-byte bootstrap nonce as lowercase hexadecimal
.SH "OMAKURE NODE AUTHORITY"
Hold and use this node's enrollment authority
.PP
\fBUsage:\fR omakure node authority [OPTIONS] <COMMAND>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE AUTHORITY CREATE"
Create this node's enrollment authority key, refusing to replace one
.PP
\fBUsage:\fR omakure node authority create [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-confirmed\fR
Required. Creating an authority is a fleet\-wide act
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE AUTHORITY SHOW"
Report the authority this node holds, without its private half
.PP
\fBUsage:\fR omakure node authority show [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE NODE AUTHORITY ISSUE"
Mint one enrollment bundle naming this node as the subject
.PP
\fBUsage:\fR omakure node authority issue [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-audience\fR \fI<AUDIENCE>\fR
The node that will apply this bundle. It is checked against that node's own identity when it does, so a bundle is useless anywhere else
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-role\fR \fI<ROLE>\fR
The role the audience will record for this node
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-capability\fR \fI<CAPABILITIES>\fR
A capability the audience will grant this node. Repeatable
.TP
\fB\-\-lifetime\-seconds\fR \fI<LIFETIME_SECONDS>\fR
How long the bundle stays valid, in seconds
.RS
\fIDefault: \fR3600
.RE
.SH "OMAKURE NODE CAPABILITIES"
Update one peer's capability allow\-list with confirmation and evidence
.PP
\fBUsage:\fR omakure node capabilities [OPTIONS] <NODE_ID>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-capability\fR \fI<CAPABILITIES>\fR
Allowed capability (repeatable; sorted unique values are required)
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-actor\fR \fI<ACTOR>\fR
Audit actor
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Audit reason/evidence
.TP
\fB\-\-confirmed\fR
Confirm this trust mutation explicitly
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<NODE_ID>\fR
Peer node identifier
.SH "OMAKURE NODE REVOKE"
Revoke one peer with confirmation and evidence
.PP
\fBUsage:\fR omakure node revoke [OPTIONS] <NODE_ID>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-actor\fR \fI<ACTOR>\fR
Audit actor
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-reason\fR \fI<REASON>\fR
Audit reason/evidence
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-confirmed\fR
Confirm this trust mutation explicitly
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<NODE_ID>\fR
Peer node identifier
.SH "OMAKURE NODE RESET"
Explicitly remove validated machine identity and node trust state
.PP
\fBUsage:\fR omakure node reset [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-confirmed\fR
Confirm destructive removal of identity and trust state
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE CONFIG"
Show resolved paths and environment diagnostics

Prints the resolved binary path, omakure version, workspace root, scripts root, `.omakure/` directory, history directory, workspace config file, environments directory, active environment, and any known env overrides (`OMAKURE_SCRIPTS_DIR`, `OMAKURE_REPO`, `OVERTURE_*`, `CLOUD_MGMT_*`, `REPO`, `VERSION`). Pass `\-\-json` for the machine\-readable envelope.
.PP
\fBUsage:\fR omakure config [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE UPDATE"
Update omakure from GitHub releases

Downloads the release archive for the current OS/arch and replaces the running binary in place. Also copies any scripts missing from your local scripts directory from the source archive of the target version — existing files are never overwritten. `\-\-repo` defaults to `$OMAKURE_REPO` / `$OVERTURE_REPO` / `$CLOUD_MGMT_REPO` / `$REPO` / `This\-Is\-NPC/omakure`; `\-\-version` defaults to `$VERSION` or the latest GitHub release.
.PP
\fBUsage:\fR omakure update [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-repo\fR \fI<REPO>\fR
GitHub repository (`owner/name`). Defaults to `$OMAKURE_REPO` / `$OVERTURE_REPO` / `$CLOUD_MGMT_REPO` / `$REPO` / `This\-Is\-NPC/omakure`
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-version\fR \fI<VERSION>\fR
Release tag to install (e.g. `v0.1.9`). Defaults to `$VERSION` or the latest GitHub release for the configured repo
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE UNINSTALL"
Remove the omakure binary (optionally wipe the scripts workspace)

Deletes the currently running binary from its install directory (on Windows also strips the install path from the user `PATH`). With `\-\-scripts`, PERMANENTLY deletes the entire scripts workspace, including `.omakure/` (envs, daemon files), `.history/`, schedules) and every script file — use with care and have backups.
.PP
\fBUsage:\fR omakure uninstall [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\fR
Also delete the scripts workspace directory (runs.sqlite, history, schedules, and every user script). Destructive
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.SH "OMAKURE COMPLETION"
Generate shell completion script for the given shell

Writes the completion script to stdout. Quick install examples:
 bash: `omakure completion bash >> ~/.bashrc`
 zsh:  `omakure completion zsh  > ~/.zfunc/_omakure` (ensure `~/.zfunc` is on `$fpath`)
 fish: `omakure completion fish > ~/.config/fish/completions/omakure.fish`
 pwsh: `omakure completion pwsh | Out\-String | Invoke\-Expression`

For a one\-shot session pipe into your current shell: `eval "$(omakure completion zsh)"`.
.PP
\fBUsage:\fR omakure completion [OPTIONS] <SHELL>
.PP
\fBOptions:\fR
.PP
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
\fBArguments:\fR
.PP
.TP
\fB<SHELL>\fR
Shell to generate completions for
.SH "OMAKURE SERVE"
Run the cron scheduler daemon for scripts declaring a `Schedule` block

Running `omakure serve` with no flags starts the scheduler in the foreground with an in\-process worker; `\-d`/`\-\-detach` daemonizes (Unix) and `\-\-stop` terminates a running daemon.

The scheduler rescans the workspace every 5 seconds, parses each script's `Schedule` block, and enqueues a run when the cron expression is due. Fires are SKIPPED when a previous run with the same `cron_schedule_id` is still `queued` or `running`, so long\-lived overlapping jobs never stack up.

Paths (per workspace):
 PID file: `<workspace>/.omakure/daemon.pid`
 Log:      `<workspace>/.omakure/daemon.log`

`\-\-install`/`\-\-uninstall`/`\-\-status` manage a per\-workspace systemd user unit so the daemon survives reboots (Linux only); after install tail with `journalctl \-\-user \-u <unit> \-f`.

By default an in\-process worker is spawned so scheduled rows execute without a separate process. Pass `\-\-no\-worker` when you run `omakure queue worker` elsewhere.

Global `\-\-json` applies to lifecycle probes (`\-\-status`, `\-\-stop`, `\-\-install`, `\-\-uninstall`). Foreground serve and `\-\-once` do not emit a JSON envelope.
.PP
\fBUsage:\fR omakure serve [OPTIONS]
.PP
\fBOptions:\fR
.PP
.TP
\fB\-d, \-\-detach\fR
Run the scheduler as a detached background daemon (Unix only)
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-scripts\-dir\fR \fI<SCRIPTS_DIR>\fR
Scripts directory override
.TP
\fB\-\-stop\fR
Stop a running daemon (reads `.omakure/daemon.pid` and sends SIGTERM)
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-json\fR
Emit machine\-readable JSON output for AI\-facing subcommands.

When set, supported subcommands print exactly one JSON envelope `{ ok, data, error, schema_version }` on stdout instead of their human\-readable form. Subcommands that do not support JSON ignore this flag.
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-install\fR
Install a systemd user service that runs `omakure serve` for the current workspace and survives reboots (Linux only)
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-uninstall\fR
Disable and remove the systemd user service for the current workspace (Linux only)
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-status\fR
Print the systemd user service status for the current workspace (Linux only)
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-no\-worker\fR
Do not spawn the in\-process worker. Use when you already run `omakure queue worker` elsewhere
.RS
\fIDefault: \fRfalse
.RE
.TP
\fB\-\-concurrency\fR \fI<CONCURRENCY>\fR
Number of worker threads for the in\-process worker (default 1)
.RS
\fIDefault: \fR1
.RE
.TP
\fB\-\-once\fR
Test convenience: run a single scheduler tick, enqueue whatever is due, then exit. Hidden from \-\-help. Used by integration tests
.RS
\fIDefault: \fRfalse
.RE
