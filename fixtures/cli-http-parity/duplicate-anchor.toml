schema_version = 1

[[entries]]
entry_id = "run"
class = "cli-only"
operation_family = "fixture"
docs_anchor = "shared"
cli_ids = ["run"]
rationale = "fixture"
[entries.adapter_only]
auth = "fixture"
lifecycle = "fixture"

[[entries]]
entry_id = "health"
class = "http-only"
operation_family = "fixture"
docs_anchor = "#shared"
http_ids = ["GET /v1/health"]
rationale = "fixture"
[entries.adapter_only]
auth = "fixture"
lifecycle = "fixture"
