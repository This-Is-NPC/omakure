schema_version = 1

[[entries]]
entry_id = "wildcard"
class = "http-only"
operation_family = "fixture"
docs_anchor = "wildcard"
http_ids = ["GET /v1/tree/*path"]
rationale = "fixture"
[entries.adapter_only]
auth = "fixture"
lifecycle = "fixture"
