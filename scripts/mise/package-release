#!/usr/bin/env bash
set -euo pipefail

root="${MISE_PROJECT_ROOT:-$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd -P)}"
root="$(cd -- "$root" && pwd -P)"
"$root/scripts/mise/build-release"
exec "$root/scripts/release/package-release.sh" "$root/target/release/omakure" "$root/dist/omakure.tar.gz" "$@"
