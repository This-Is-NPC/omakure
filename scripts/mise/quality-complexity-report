#!/usr/bin/env bash
set -euo pipefail

root="${MISE_PROJECT_ROOT:-$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd -P)}"
root="$(cd -- "$root" && pwd -P)"
cd "$root"
exec "$root/scripts/tasks/complexity-report" --source-root . --output target/quality/complexity-report.json "$@"
