#!/usr/bin/env bash
set -euo pipefail

root="${MISE_PROJECT_ROOT:-$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd -P)}"
root="$(cd -- "$root" && pwd -P)"
cd "$root"
exec cargo test --test direct_transport_e2e --test node_service_e2e --test http_api_e2e --test cli_surface_e2e --test policy_e2e "$@"
