#!/usr/bin/env bash
set -euo pipefail

root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." && pwd -P)"
cd "$root"
script="$root/scripts/tasks/coverage"
bounded="$root/scripts/tasks/atomic/run-bounded"
[[ -x "$script" && -x "$bounded" ]] || { printf 'atomic/coverage: required scripts are missing\n' >&2; exit 1; }
exec "$bounded" 45m "$script" "$@"
