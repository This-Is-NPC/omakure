#!/usr/bin/env bash
set -euo pipefail

root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." && pwd -P)"
cd "$root"
command -v cargo >/dev/null 2>&1 || { printf 'atomic/test-bins: cargo is required\n' >&2; exit 1; }
bounded="$root/scripts/tasks/atomic/run-bounded"
[[ -x "$bounded" ]] || { printf 'atomic/test-bins: bounded runner is missing\n' >&2; exit 1; }
exec "$bounded" 30m cargo test --bins --locked "$@"
