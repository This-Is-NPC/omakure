#!/usr/bin/env bash
set -euo pipefail
root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." && pwd -P)"
"$root/scripts/tasks/cert/transport" "$@"
"$root/scripts/tasks/cert/health" "$@"
exec "$root/scripts/tasks/suite/cert-vm" "$@"
